<h1>投稿一覧</h1>

<% @posts.each do |post| %>
  <div>
    <h3>
      <%= link_to post.title, post_path(post) %>
    </h3>

    <p><%= post.body %></p>
  </div>

  <% if post.image.attached? %>
    <%= image_tag post.image %>
  <% end %>

<% end %>