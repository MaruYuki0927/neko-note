<h1><%= @cat.name %> のページ</h1>

<h2>投稿一覧</h2>

<% @posts.each do |post| %>
  <div>
    <p>タイトル：<%= post.title %></p>
    <p>内容：<%= post.body %></p>

    <% if post.image.attached? %>
      <%= image_tag post.image, size: "200x200" %>
    <% end %>

    <hr>
  </div>
<% end %>